# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example


# Import configs (may contain sensitive URLs)
imports/*.json

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Data exports from cli/backup.ts — contain personal data, never commit
backups

# Telegram-Einladungen: ein privater Invite-Link IST die Berechtigung,
# gehoert also nicht ins Repository. data/telegram-channels.example.json
# dokumentiert das Format und bleibt eingecheckt.
data/telegram-channels.json
